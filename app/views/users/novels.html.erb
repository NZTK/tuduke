<%= render "common/header" %>
<div class= "body">
	<div class= "container">
		<div class= "show_head">
 			<h3><%= @user.username %>さんの投稿作品一覧</h3>
 		</div>
 		<% if @novels.blank? %>
 		<div class= "box_head">
 			<h2>投稿作品はありません</h2>
 		</div>
 		<%  else %>
		<div class= "row">
			<div class= "col-lg-6 col-lg-offset-3 col-md-6 col-md-3 col-xs-10 col-xs-offset-1" >
				<% @novels.each do |f| %>
					<div class= "novel-box">
						<h3><%= link_to simple_format(f.novel_title), novel_path(f.id) %></h3>
						<button class= "genre"><%= link_to f.genre.genre_name ,novels_path(genre_id: f.genre.id) %></button><br>
						<% f.tag_list.each do |tag|  %>
							<button class="tag">
								<%= link_to tag, novels_path(tag_name: tag) %>
							</button>
						<% end %>
						<div class ="open">
							<div class= "novel-about">
							<%= simple_format(f.novel_about) %>
							</div>
						</div>
						作成日：<%= f.created_at.strftime('%Y/%m/%d') %>
						最終更新日：<%= f.updated_at.strftime('%Y/%m/%d') %><br>
						参加ユーザー<% f.novel_content.map(&:user).uniq.each do |u| %>
						<%= link_to u.username, user_path(u) %>
						<% end  %>
					</div>
				<% end %>
				<%= paginate @novels %>
			</div>
		</div>
		<% end %>
	</div>
</div>